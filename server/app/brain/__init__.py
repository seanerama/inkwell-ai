"""Per-space brain store (stub in Stage 1).

Retrieval and ``brain_writes`` persistence (SPEC §6.2, §10.3, Phase 5) land later.
The ``brain_entries`` table exists from migration 0001 so the schema is frozen early.
"""
