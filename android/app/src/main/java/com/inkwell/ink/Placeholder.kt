/**
 * Ink capture, one-euro filtering, and the in-memory stroke model (SPEC §9.2).
 *
 * Placeholder for the walking skeleton (Stage 2): no capture surface exists yet.
 * The persisted stroke shape and its packed-points codec are frozen now in
 * `com.inkwell.data` (contract `ink-storage`); capture lands in a later stage.
 */
package com.inkwell.ink
